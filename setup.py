from distutils.core import setup

setup(
    name="blip-model", 
    version="v1.0.0",
    packages=find_packages(exclude=["tests"]),
    install_requires=[
    ],
    entry_points={
    },
    package_data={
    },
)
